# Configuración base del framework
framework {
  name = "Walmart Performance Framework"
  version = "1.0"
}

# Configuración por ambiente
environments {
  # Walmart environments
  walmart {
    baseUrl = "https://perfappdemo.vercel.app"
    users = 10
    duration = 60
    rampUp = 30
    username = "llabarca"
    password = "Vdt*y3xDSys4"
  }

  walmart-sanity {
    baseUrl = "https://perfappdemo.vercel.app"
    users = 1
    duration = 10
    rampUp = 1
    username = "llabarca"
    password = "Vdt*y3xDSys4"
  }

  walmart-load {
    baseUrl = "https://perfappdemo.vercel.app"
    users = 50
    duration = 300
    rampUp = 60
    username = "llabarca"
    password = "Vdt*y3xDSys4"
  }

  # Demo/Practice environments
  demo {
    baseUrl = "https://jsonplaceholder.typicode.com"
    users = 10
    duration = 60
    rampUp = 20
  }

  demo-sanity {
    baseUrl = "https://jsonplaceholder.typicode.com"
    users = 1
    duration = 10
    rampUp = 1
  }

  reqres {
    baseUrl = "https://reqres.in/api"
    users = 15
    duration = 90
    rampUp = 30
  }

  reqres-sanity {
    baseUrl = "https://reqres.in/api"
    users = 1
    duration = 10
    rampUp = 1
  }
}

# Configuración de thresholds
thresholds {
  responseTime {
    p95 = 2000
    p99 = 3000
  }

  successRate = 95.0
}

# Configuración de Prometheus (para después)
prometheus {
  enabled = false
  port = 9090
  pushGateway = "localhost:9091"
}